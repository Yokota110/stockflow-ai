[build]
builder = "RAILPACK"
buildCommand = "pnpm install && pnpm --filter @stockflow/api... build"

[deploy]
startCommand = "pnpm --filter @stockflow/api start:prod"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
